"""The correctness core: identifiers, the page record, the index schema, and the exact surface.

Everything provable without a model call lives here, and M1 proves it on synthetic text before a
cent is spent (Spec §13).
"""
