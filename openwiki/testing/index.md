# Files

- [Test Layers, Conformance and CI](test-layers-and-ci.md) - The five test layers and the scripts that run them, the conformance greps that enforce the architectural rules no compiler can, the Playwright end-to-end stack, and what CI runs on every commit with no credential anywhere.
