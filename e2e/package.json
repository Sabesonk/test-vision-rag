{
  "name": "vsir-e2e",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "The Playwright replay suite (Spec §13 M7, U024) — the console, driven against the stack scripts/test-e2e.sh brings up.",
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0"
  }
}
